<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.djw.dagger2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="design_width"
            android:value="720" />
        <meta-data
            android:name="design_height"
            android:value="1280" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.zhihu.activity.PaperInfoActivity"
            android:theme="@style/NoStateBar" />
        <activity
            android:name=".ui.zhihu.activity.CommentActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.zhihu.activity.ThemInfoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.zhihu.activity.SectionActivity" />
        <activity android:name=".ui.wx.activity.WebviewActivity" />
        <activity
            android:name=".ui.wx.activity.SearchActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>